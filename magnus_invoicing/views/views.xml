<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="magnus_invoicing.list">
      <field name="name">magnus_invoicing list</field>
      <field name="model">magnus_invoicing.magnus_invoicing</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="magnus_invoicing.action_window">
      <field name="name">magnus_invoicing window</field>
      <field name="res_model">magnus_invoicing.magnus_invoicing</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="magnus_invoicing.action_server">
      <field name="name">magnus_invoicing server</field>
      <field name="model_id" ref="model_magnus_invoicing_magnus_invoicing"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="magnus_invoicing" id="magnus_invoicing.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="magnus_invoicing.menu_1" parent="magnus_invoicing.menu_root"/>
    <menuitem name="Menu 2" id="magnus_invoicing.menu_2" parent="magnus_invoicing.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="magnus_invoicing.menu_1_list" parent="magnus_invoicing.menu_1"
              action="magnus_invoicing.action_window"/>
    <menuitem name="Server to list" id="magnus_invoicing" parent="magnus_invoicing.menu_2"
              action="magnus_invoicing.action_server"/>
    -->
  </data>
</odoo>
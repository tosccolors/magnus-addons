Account Customizations (Magnus)
===============================

This module adds the following changes to account:

- don't set accounting date depending on detected sequence when changing invoice date, always use invoice date for onchange
- allow cutoff for any kind of move
